# permissionMode 可选值

在 `settings.local.json` 的 `permissions.permissionMode` 中手动修改：

| 值 | 说明 |
|---|---|
| `"default"` | 默认，每次操作需确认 |
| `"auto"` | 自动接受工具调用 |
| `"plan"` | 计划模式 |
| `"bypassPermissions"` | 跳过所有权限检查 |
